#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../nodester-cli/bin/nodester.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../nodester-cli/bin/nodester.js" "$@"
  ret=$?
fi
exit $ret
